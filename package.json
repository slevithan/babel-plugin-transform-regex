{
  "name": "babel-plugin-transform-regex",
  "version": "3.0.0",
  "description": "Babel plugin for the regex package",
  "author": "Steven Levithan",
  "license": "MIT",
  "type": "module",
  "exports": "./src/index.js",
  "scripts": {
    "build": "esbuild src/index.js --bundle --minify --outfile=dist/standalone.min.js --global-name=BabelPluginTransformRegexStandalone",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "src",
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/slevithan/babel-plugin-transform-regex.git"
  },
  "keywords": [
    "regex",
    "babel-plugin"
  ],
  "dependencies": {
    "regex": "^3.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
